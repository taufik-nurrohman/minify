/** drop space after `(` and before `)` */
asdf { asdf: asdf ( asdf / asdf ) ; asdf: asdf; }

/** drop space after `(` and before `)` */
asdf { asdf: asdf  (  asdf / asdf  )  ; asdf: asdf; }

/** drop space after `(` and before `)` */
asdf { asdf: asdf
  (
    asdf / asdf
  )
; asdf: asdf; }